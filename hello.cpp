#include <iostream>
using namespace std;

int main(){

    /*cout<<"hello"<<endl;

    int a=123;
    cout<<a<<endl;

    char b='a'; //single quotes for char
    cout<<b<<endl;

    bool bl=true;
    cout<<bl<<endl; //returns 1 or 0 not true,false

    float f=1.2;
    cout<<f<<endl;

    double d=1.23;
    cout<<d<<endl;

    string str="hello";
    cout<<str<<endl;
    
    int s1=sizeof(a);
    int s2=sizeof(b);
    int s3=sizeof(bl);
    int s4=sizeof(f);
    int s5=sizeof(d);
    int s6=sizeof(str);


    cout<<s1<<endl; //int
    cout<<s2<<endl; //char
    cout<<s3<<endl; //bool
    cout<<s4<<endl; //float
    cout<<s5<<endl; //double
    cout<<s6<<endl; //string

    int a='a'; //typecasting
    cout<<a<<endl;

    char ch=98; //the integer 98 is typecasted into the char variable ch and its ascii value is printed
    cout<<ch<<endl;

    char ch1=123456; //takes only last 8 bits and not the whole and print the ascii value of that last 8 bits(here 64)
    cout<<ch1<<endl;

    int b=-5; //negative integer
    cout<<b<<endl;

    unsigned int a=-3; // if we write negative for unsigned then we get the 2's complement value of 3 that is some big integer in decimal
    cout<<a<<endl;
    cout<<sizeof(a);

    int a,b;
    a=6/10; // use of / gives values before decimal point and doesn't rounds off
    b=6/10.0;//use of .0 gives exact result after decimal
    //write anything above, we will get only value before decimal point (integer value is the return type)

    cout << a << endl;
    cout << b << endl;

    float c,d,e,f;
    c=7/10; //0
    d=7.0/10; //0.7...same for 7.00
    e=7/10.0; //0.7...same for 10.00
    f=7.0/10.0; //0.7

    cout<<c<<endl;
    cout<<d<<endl;
    cout<<e<<endl;
    cout<<f<<endl;

    int a=9; //if a=0.9 then prints 0
    float b=10;
    cout<<a<<endl; // prints 9 when a=9 and also when a=9.0
    cout<<b<<endl; // prints 10 when b=10 and not 10.0.....prints 10 when b=10.0
    cout<<a/b<<endl;*/

    int a=2;
    int b=3;
    bool bl = (a!=b);
    cout<<bl<<endl;

    return 0;

 }